import WishListCard from "@/components/WishListCard";
import { ReactElement } from "react";

export default function WishListPage(): ReactElement {
  return (
    <main className="flex justify-center py-25">
      <WishListCard />
    </main>
  );
}
